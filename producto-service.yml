server:
  port: 8081

spring:
  application:
    name: producto-service

  datasource:
    url: jdbc:mysql://localhost:3307/producto_db
    username: bruno
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cloud:
    config:
      enabled: false   # 🚫 No usamos Config Server todavía

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
